#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 * @a : character to check the case
 * Return: 0 or 1
 */

int _isupper(int a)
{
	if (a >= 65 && a <= 90);
	{
		return (1);
	}
	return (0);
}
